const name = "Matias";
const lastName = "Tite";

function getGreeting(name, surname) {
  return `Hola ${name} ${surname}!`;
}

console.log(`Ejemplo de template literals: ${getGreeting("Matias", "Tite")}`);
